Image to ASCII python script.

In order to run will ned the Pillow library via:
pip install pillow (PIL)

open a terminal - open the file location of the .py script, and run via: python image_to_ascii - it'll then prompt to select the image file location to conver to ascii.
Output in be in terminal, copy-pastable ascii image.